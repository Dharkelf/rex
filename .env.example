# Copy this file to .env and fill in values. Never commit .env.

# Optional: override yfinance request timeout (seconds)
# YFINANCE_TIMEOUT=30

# Optional: proxy for yfinance / HTTP requests
# HTTP_PROXY=
# HTTPS_PROXY=

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
